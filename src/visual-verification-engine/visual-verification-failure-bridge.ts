/**
 * Visual verification failure bridge — re-exports failure context builder.
 */

export {
  buildVisualVerificationFailureContext,
  type VisualVerificationFailureContext,
} from './visual-verification-report.js';
