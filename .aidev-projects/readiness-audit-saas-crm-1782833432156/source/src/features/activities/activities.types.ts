/** Types for activities feature module — Custom App */
export interface ActivitiesRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface ActivitiesFormState {
  label: string;
}
