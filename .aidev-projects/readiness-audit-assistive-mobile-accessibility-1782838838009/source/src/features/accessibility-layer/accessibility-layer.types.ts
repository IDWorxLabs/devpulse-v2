/** Types for accessibility-layer feature module — LISA — Locked In Syndrome App */
export interface AccessibilityLayerRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface AccessibilityLayerFormState {
  label: string;
}
