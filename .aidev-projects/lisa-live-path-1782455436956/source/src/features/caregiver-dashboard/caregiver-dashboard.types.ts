/** Types for caregiver-dashboard feature module — LISA — Locked In Syndrome App */
export interface CaregiverDashboardRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface CaregiverDashboardFormState {
  label: string;
}
