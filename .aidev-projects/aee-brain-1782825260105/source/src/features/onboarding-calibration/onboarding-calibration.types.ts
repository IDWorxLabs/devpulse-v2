/** Types for onboarding-calibration feature module — LISA — Locked In Syndrome App */
export interface OnboardingCalibrationRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface OnboardingCalibrationFormState {
  label: string;
}
