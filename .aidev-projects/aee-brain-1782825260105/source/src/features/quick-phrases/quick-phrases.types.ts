/** Types for quick-phrases feature module — LISA — Locked In Syndrome App */
export interface QuickPhrasesRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface QuickPhrasesFormState {
  label: string;
}
