/** Types for reports feature module — Expense Tracker */
export interface ReportsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface ReportsFormState {
  label: string;
}
