/** Types for accessibility-settings feature module — LISA — Locked In Syndrome App */
export interface AccessibilitySettingsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface AccessibilitySettingsFormState {
  label: string;
}
