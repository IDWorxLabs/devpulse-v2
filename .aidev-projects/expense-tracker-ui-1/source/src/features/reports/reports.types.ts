/** Types for reports feature module — modern expense tracking */
export interface ReportsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface ReportsFormState {
  label: string;
}
