/** Types for income feature module — modern expense tracking */
export interface IncomeRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface IncomeFormState {
  label: string;
}
