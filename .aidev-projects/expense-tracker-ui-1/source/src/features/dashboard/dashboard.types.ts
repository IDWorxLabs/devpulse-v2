/** Types for dashboard feature module — modern expense tracking */
export interface DashboardRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface DashboardFormState {
  label: string;
}
