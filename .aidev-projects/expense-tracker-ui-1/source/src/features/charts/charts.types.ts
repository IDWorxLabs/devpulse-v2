/** Types for charts feature module — modern expense tracking */
export interface ChartsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface ChartsFormState {
  label: string;
}
