/** Types for locked-in feature module — LISA — Locked In Syndrome App */
export interface LockedInRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface LockedInFormState {
  label: string;
}
