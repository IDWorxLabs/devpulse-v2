/** Types for navigation-router feature module — Expense Tracker */
export interface NavigationRouterRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface NavigationRouterFormState {
  label: string;
}
