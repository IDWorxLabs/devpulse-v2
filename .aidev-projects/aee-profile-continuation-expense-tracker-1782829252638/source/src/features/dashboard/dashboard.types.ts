/** Types for dashboard feature module — Expense Tracker */
export interface DashboardRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface DashboardFormState {
  label: string;
}
