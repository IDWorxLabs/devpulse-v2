/** Types for emergency-speech feature module — LISA — Locked In Syndrome App */
export interface EmergencySpeechRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface EmergencySpeechFormState {
  label: string;
}
