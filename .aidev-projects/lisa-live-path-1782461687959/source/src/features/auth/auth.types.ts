/** Types for auth feature module — LISA — Locked In Syndrome App */
export interface AuthRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface AuthFormState {
  label: string;
}
