/** Types for text-to-speech feature module — LISA — Locked In Syndrome App */
export interface TextToSpeechRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface TextToSpeechFormState {
  label: string;
}
