/** Types for gaze-keyboard feature module — LISA — Locked In Syndrome App */
export interface GazeKeyboardRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface GazeKeyboardFormState {
  label: string;
}
