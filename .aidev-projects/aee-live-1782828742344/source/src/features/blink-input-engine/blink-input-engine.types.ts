/** Types for blink-input-engine feature module — LISA — Locked In Syndrome App */
export interface BlinkInputEngineRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface BlinkInputEngineFormState {
  label: string;
}
