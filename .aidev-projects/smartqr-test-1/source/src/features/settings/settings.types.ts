/** Types for settings feature module — Qr Code Scanning */
export interface SettingsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface SettingsFormState {
  label: string;
}
