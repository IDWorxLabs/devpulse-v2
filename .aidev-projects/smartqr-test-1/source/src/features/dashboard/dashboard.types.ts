/** Types for dashboard feature module — Qr Code Scanning */
export interface DashboardRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface DashboardFormState {
  label: string;
}
