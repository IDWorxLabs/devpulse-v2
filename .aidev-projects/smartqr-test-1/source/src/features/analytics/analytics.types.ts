/** Types for analytics feature module — Qr Code Scanning */
export interface AnalyticsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface AnalyticsFormState {
  label: string;
}
