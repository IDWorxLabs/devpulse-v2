/** Types for calculator feature module — calculator */
export interface CalculatorRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface CalculatorFormState {
  label: string;
}
