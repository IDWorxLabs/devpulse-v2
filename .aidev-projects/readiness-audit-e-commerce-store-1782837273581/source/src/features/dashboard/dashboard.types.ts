/** Types for dashboard feature module — Custom App */
export interface DashboardRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface DashboardFormState {
  label: string;
}
