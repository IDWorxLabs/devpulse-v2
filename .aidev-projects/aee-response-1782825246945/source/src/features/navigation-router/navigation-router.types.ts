/** Types for navigation-router feature module — LISA — Locked In Syndrome App */
export interface NavigationRouterRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface NavigationRouterFormState {
  label: string;
}
