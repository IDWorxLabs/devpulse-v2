/** Types for eye-tracking-board feature module — LISA — Locked In Syndrome App */
export interface EyeTrackingBoardRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface EyeTrackingBoardFormState {
  label: string;
}
