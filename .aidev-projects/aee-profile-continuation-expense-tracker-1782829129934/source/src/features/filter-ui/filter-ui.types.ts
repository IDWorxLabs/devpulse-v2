/** Types for filter-ui feature module — Expense Tracker */
export interface FilterUiRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface FilterUiFormState {
  label: string;
}
