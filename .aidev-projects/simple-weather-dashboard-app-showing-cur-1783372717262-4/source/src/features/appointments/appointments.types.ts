/** Types for appointments feature module — modern */
export interface AppointmentsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface AppointmentsFormState {
  label: string;
}
