/** Types for responsive feature module — modern */
export interface ResponsiveRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface ResponsiveFormState {
  label: string;
}
