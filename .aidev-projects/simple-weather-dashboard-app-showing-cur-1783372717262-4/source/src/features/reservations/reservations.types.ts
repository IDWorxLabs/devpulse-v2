/** Types for reservations feature module — modern */
export interface ReservationsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface ReservationsFormState {
  label: string;
}
