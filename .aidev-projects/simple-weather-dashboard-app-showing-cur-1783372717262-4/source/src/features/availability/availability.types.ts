/** Types for availability feature module — modern */
export interface AvailabilityRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface AvailabilityFormState {
  label: string;
}
