/** Types for navigation-router feature module — Custom App */
export interface NavigationRouterRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface NavigationRouterFormState {
  label: string;
}
