/** Types for filter-ui feature module — LISA — Locked In Syndrome App */
export interface FilterUiRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface FilterUiFormState {
  label: string;
}
