/** Types for calendar feature module — reusable components where */
export interface CalendarRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface CalendarFormState {
  label: string;
}
