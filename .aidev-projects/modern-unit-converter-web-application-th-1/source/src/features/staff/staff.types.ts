/** Types for staff feature module — reusable components where */
export interface StaffRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface StaffFormState {
  label: string;
}
