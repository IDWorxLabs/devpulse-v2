/** Types for taxes feature module — reusable components where */
export interface TaxesRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface TaxesFormState {
  label: string;
}
