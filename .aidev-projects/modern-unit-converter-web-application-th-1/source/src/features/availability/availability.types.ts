/** Types for availability feature module — reusable components where */
export interface AvailabilityRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface AvailabilityFormState {
  label: string;
}
