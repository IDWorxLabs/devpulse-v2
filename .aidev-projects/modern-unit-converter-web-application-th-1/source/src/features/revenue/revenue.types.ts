/** Types for revenue feature module — reusable components where */
export interface RevenueRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface RevenueFormState {
  label: string;
}
