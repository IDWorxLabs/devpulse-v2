/** Types for currency feature module — reusable components where */
export interface CurrencyRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface CurrencyFormState {
  label: string;
}
