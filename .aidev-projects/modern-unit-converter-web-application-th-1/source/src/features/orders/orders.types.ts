/** Types for orders feature module — reusable components where */
export interface OrdersRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface OrdersFormState {
  label: string;
}
