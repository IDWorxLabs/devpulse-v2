/** Types for appointments feature module — reusable components where */
export interface AppointmentsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface AppointmentsFormState {
  label: string;
}
