/** Types for customers feature module — reusable components where */
export interface CustomersRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface CustomersFormState {
  label: string;
}
