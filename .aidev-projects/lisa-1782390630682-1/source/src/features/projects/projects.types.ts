/** Types for projects feature module — Lisa As A Real Modular */
export interface ProjectsRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface ProjectsFormState {
  label: string;
}
