/** Types for dashboard feature module — Lisa As A Real Modular */
export interface DashboardRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface DashboardFormState {
  label: string;
}
