/** Types for timeline feature module — Lisa As A Real Modular */
export interface TimelineRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface TimelineFormState {
  label: string;
}
