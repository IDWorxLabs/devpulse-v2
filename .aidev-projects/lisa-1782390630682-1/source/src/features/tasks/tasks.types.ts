/** Types for tasks feature module — Lisa As A Real Modular */
export interface TasksRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface TasksFormState {
  label: string;
}
