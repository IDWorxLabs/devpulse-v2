/** Types for navigation-router feature module — modern */
export interface NavigationRouterRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface NavigationRouterFormState {
  label: string;
}
