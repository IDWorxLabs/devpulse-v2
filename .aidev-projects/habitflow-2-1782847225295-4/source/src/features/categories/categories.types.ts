/** Types for categories feature module — modern */
export interface CategoriesRecord {
  id: string;
  label: string;
  createdAt: string;
}

export interface CategoriesFormState {
  label: string;
}
